# 🚀 QUICK START - Bước Tiếp Theo

## ⚠️ Bạn cần cài Node.js trước!

### Bước 1: Cài đặt Node.js
1. **Vào trang:** https://nodejs.org/
2. **Download:** Chọn phiên bản LTS (Recommended - màu xanh)
3. **Cài đặt:** Chạy file .msi vừa download
   - ✅ Check "Automatically install the necessary tools"
   - ✅ Tiếp tục Next → Install
4. **Khởi động lại** terminal/PowerShell

### Bước 2: Verify đã cài xong
Mở PowerShell/Terminal mới và chạy:
```bash
node --version
npm --version
```
Nếu hiện version (VD: v18.17.0) là OK! ✅

### Bước 3: Cài dependencies
Trong terminal, ở thư mục project:
```bash
cd "c:\Project meme\Journey Wellnes hackathon"
npm install
```

### Bước 4: Chạy app
```bash
npm start
```

App sẽ tự mở browser tại **http://localhost:3000** 🎉

---

## 📝 TÓM TẮT NHANH:

1. **Cài Node.js:** https://nodejs.org/ (LTS version)
2. **Restart terminal**
3. **cd vào project folder**
4. **npm install**
5. **npm start**
6. **Mở browser:** http://localhost:3000

---

## 🔍 Kiểm tra nhanh

Sau khi cài Node.js, chạy lệnh này để verify:
```bash
node --version
```

Nếu hiện version number = OK! Tiếp tục bước 3.

---

**Lưu ý:** Nếu bạn dùng VS Code, có thể:
- Mở folder project trong VS Code
- Mở terminal tích hợp (Ctrl + `)
- Chạy `npm install` và `npm start` từ đó



